[Tests]
  [shl_channel_cimode1]
    type = CSVDiff
    input = nek1.i
    csvdiff = nek_out.csv
    cli_args = '--nekrs-cimode 1'

    max_parallel = 20
    max_time = 900

    requirement = 'The system shall compute the velocity error norms for the shear-layer channel solution on an unrotated mesh.'
  []

  [shl_channel_cimode2]
    type = CSVDiff
    input = nek2.i
    csvdiff = nek_out.csv
    cli_args = '--nekrs-cimode 2'

    max_parallel = 20
    max_time = 900

    requirement = 'The system shall compute the velocity error norms for the shear-layer channel solution on a rotated mesh.'
  []
[]
