[Tests]
  [conj_ht_cimode1]
    type = CSVDiff
    input = nek1.i
    csvdiff = nek_out.csv
    cli_args = '--nekrs-cimode 1'

    max_parallel = 20
    max_time = 300

    requirement = 'The system shall compute the velocity and temperature error norms for the manufactured conjugate heat-transfer solution.'
    required_objects = 'NekRSProblem'
  []

  [conj_ht_cimode2]
    type = CSVDiff
    input = nek2.i
    csvdiff = nek_out.csv
    cli_args = '--nekrs-cimode 2'

    max_parallel = 20
    max_time = 300

    requirement = 'The system shall compute the velocity and temperature error norms for the manufactured conjugate heat-transfer solution using an alternative solver configuration.'
    required_objects = 'NekRSProblem'
  []
[]
