[Tests]
  [bfs]
    type = CSVDiff
    input = nek.i
    csvdiff = nek_out.csv
    cli_args = '--nekrs-cimode 1'

    # mesh only contains 60 elements
    max_parallel = 10

    requirement = 'The system shall compute the skin friction coefficient and compare it against experimental values.'
    required_objects = 'NekRSProblem'
  []
[]
